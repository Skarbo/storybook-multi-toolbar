import type { MultiToolbarParameters } from './types';

export const ADDON_ID = 'skarbo/multi-toolbar';
export const PARAM_KEY = 'multiToolbar';

export const DEFAULT_CONFIG: MultiToolbarParameters = {
  disable: false,
  toolbars: [],
};
